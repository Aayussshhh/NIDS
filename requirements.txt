# =============================================================================
# Python dependencies for the Hybrid NIDS backend.
#
# For LOCAL development with GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# For DEPLOYMENT (Render/Railway - CPU only):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# The render.yaml build command handles this automatically.
# =============================================================================

# --- Tabular ML
xgboost>=2.0.3,<3.0
scikit-learn>=1.4.0,<2.0
imbalanced-learn>=0.12.0,<1.0

# --- Explainability
shap>=0.45.0,<1.0

# --- Data handling
pandas>=2.1.0,<3.0
numpy>=1.26.0,<2.0
joblib>=1.3.0,<2.0

# --- Network capture (not needed in deployed mode, but kept for local dev)
scapy>=2.5.0,<3.0

# --- Web framework
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
pydantic>=2.5.0,<3.0
websockets>=12.0,<13.0

# --- Utilities
python-multipart>=0.0.9
typing-extensions>=4.10.0
