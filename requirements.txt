# =============================================================================
# Python dependencies for the Hybrid NIDS backend.
#
# Install with the script that picks the right PyTorch wheel for your CUDA
# (see INSTALL.md). DO NOT just run `pip install -r requirements.txt` for
# torch - it will pull the CPU-only wheel.
# =============================================================================

# --- Deep learning (install with --index-url for CUDA wheels - see INSTALL.md)
# torch==2.4.1+cu121
# torchvision==0.19.1+cu121

# --- Tabular ML (GPU-capable on Windows)
xgboost>=2.0.3,<3.0
scikit-learn>=1.4.0,<2.0
imbalanced-learn>=0.12.0,<1.0

# --- Explainability
shap>=0.45.0,<1.0

# --- Data handling
pandas>=2.1.0,<3.0
numpy>=1.26.0,<2.0
joblib>=1.3.0,<2.0

# --- Network capture (Windows: requires Npcap installed separately)
scapy>=2.5.0,<3.0

# --- Web framework
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
pydantic>=2.5.0,<3.0
websockets>=12.0,<13.0

# --- Utilities
python-multipart>=0.0.9    # for file upload endpoints if added later
typing-extensions>=4.10.0
